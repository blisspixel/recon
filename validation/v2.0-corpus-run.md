# v2.0 real-corpus run results

> **Status: <TO BE POPULATED>.** This is the result shell shipped
> in v1.9.11. The maintainer runs the aggregator against the
> gitignored private corpus per
> `validation/v2.0-corpus-run-runbook.md` and replaces each
> placeholder below with the actual number.
>
> The v2.0 release workflow refuses to tag if this file still
> contains `<TO BE POPULATED>`.

## Run metadata

- **Date:** `<TO BE POPULATED>` (UTC date the corpus scan ran)
- **Corpus snapshot:** `<TO BE POPULATED>` (timestamp suffix of
  `validation/runs-private/<UTC-stamp>/`)
- **Build under test:** v2.0 release candidate (v1.9.11 + any
  v1.9.11.x patches at run time)
- **Corpus size:** `<TO BE POPULATED>` domains

## Aggregate firing rates (real corpus)

- **Multi-cloud rollup fires on:** `<TO BE POPULATED>` of `<TO BE POPULATED>` (=`<%>`)
- **Passive-DNS ceiling fires on:** `<TO BE POPULATED>` of `<TO BE POPULATED>` (=`<%>`)

## Per-stratum firing rates (real corpus)

| Stratum | counted | multi-cloud fires | rate | ceiling fires | rate |
|---|---|---|---|---|---|
| `<populate per-stratum from aggregate.json>` | | | | | |

## Comparison to synthetic-corpus shape

The synthetic 79-fixture corpus
(`validation/synthetic_corpus/aggregate.json`, v1.9.11
re-aggregation) reports:

- Multi-cloud 29.1% fired, ceiling 86.1% fired overall.
- Per-stratum (10 fixtures each except baseline=19): baseline 42% /
  GCP 30% / Azure 10% / Oracle 30% / Alibaba 10% / PaaS 70% /
  SSE 0% multi-cloud.

The real-corpus shape should be **within ±15 percentage points
per stratum** for the synthetic corpus to count as a useful
proxy. Outside that envelope, document the divergence and the
hypothesis for why.

- **Per-stratum divergence vs synthetic:** `<TO BE POPULATED>`
  with explanation per stratum where divergence exceeds 15
  percentage points.

## `okta_idp` corpus-exposure verdict

The v1.9.5 stability table marked `okta_idp` as `not yet` because
the v1.9.0 corpus firing count was 7 (below N≥10 threshold).
v1.9.10's synthetic stratified corpus brought the synthetic
firing count above 10, but only the real corpus produces the
authoritative criterion-(c) clearance number.

- **`okta_idp` firing count on real corpus:** `<TO BE POPULATED>`
  (target: ≥ 10 for criterion-(c) clearance).
- **Disposition confirmed for v2.0:** `<keep | deprecate | split>`
  (default per v1.9.11 decision: keep; the field here records
  whether the real-corpus number supports the default).

## Reproducibility

The run shell above was populated from the aggregate output
emitted by `validation/corpus_aggregator.py` on the date listed.
The aggregator's behaviour is pinned by
`tests/test_corpus_aggregator.py`. Future v2.x patches re-run the
same workflow against an updated corpus snapshot.
