{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "recon": {
      "type": "stdio",
      "command": "recon",
      "args": [
        "mcp"
      ],
      "env": {
        "RECON_CONFIG_DIR": "${PLUGIN_DATA}/recon"
      }
    }
  }
}
